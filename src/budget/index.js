'use strict';

/**
 * Ponto de entrada público do módulo Budget.
 * Expõe apenas o router — a camada de infraestrutura fica encapsulada.
 */
const router = require('./budget.routes');

module.exports = router;
